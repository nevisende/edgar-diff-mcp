# Real-output examples

These files are generated by `npm run examples` from real SEC filings. The script constructs the real server with `buildServer()` and calls it through the MCP SDK's linked in-memory transport, so each JSON file records exactly the `structuredContent` an MCP client receives. CLI examples are captured from the same engine via `src/cli.ts`; ANSI styling is removed. No output is manually truncated.

Set `EDGAR_USER_AGENT` to a descriptive value containing an email address. `EDGAR_CACHE_DIR` is optional but recommended; filing documents are immutable and can be reused, while live EDGAR indexes are refreshed.

| File | Tool | Input summary | What to notice |
|---|---|---|---|
| [MCP JSON](mcp/01-resolve-company.json)<br>[CLI text](cli/01-resolve-company.txt) | resolve_company | `query: "AAPL"` | An exact ticker resolves to a padded CIK that can be passed directly to the other tools. |
| [MCP JSON](mcp/02-list-filings-aapl.json)<br>[CLI text](cli/02-list-filings-aapl.txt) | list_filings | AAPL 10-K filings, limit 3 | Each filing includes the accession and immutable SEC Archives URL needed by later calls. |
| [MCP JSON](mcp/03-list-items-msft.json)<br>[CLI text](cli/03-list-items-msft.txt) | list_items | MSFT latest 10-K (0001193125-26-323660) | The filing is parsed once into confidently located Items with paragraph counts and warnings. |
| [MCP JSON](mcp/04-get-section-aapl-risk-factors.json) | get_section | AAPL 0000320193-25-000079, Item 1A, 6 paragraphs | Every verbatim paragraph carries its own accession, Item, paragraph index, and SEC URL citation. |
| [MCP JSON](mcp/05-diff-sections-aapl-risk-factors.json) | diff_sections | AAPL Item 1A, FY2024 → FY2025, 6 changes with word diffs | Changed paragraphs retain citations for both sides and opt-in word-level edit runs. |
| [MCP JSON](mcp/06-diff-all-items-tsla.json)<br>[CLI text](cli/06-diff-all-items-tsla.txt) | diff_all_items | TSLA two latest 10-Ks (0001628280-25-003063 → 0001628280-26-003952) | The statistics-only overview ranks Items by change without pulling bulky paragraph text. |
| [MCP JSON](mcp/07-search-filing-aapl-tariffs.json)<br>[CLI text](cli/07-search-filing-aapl-tariffs.txt) | search_filing | AAPL 0000320193-25-000079, Item 7, /tariff/i, limit 5 | Search returns full matching paragraphs verbatim, each with a citation rather than an isolated snippet. |
| [MCP JSON](mcp/08-get-section-ge-not-found.json)<br>[CLI text](cli/08-get-section-ge-not-found.txt) | get_section | GE latest 10-K (0000040545-26-000008), Item 1A | `not_found` includes `availableItems` instead of returning a guessed section. |
| [MCP JSON](mcp/09-diff-sections-brk-risk-factors.json)<br>[CLI text](cli/09-diff-sections-brk-risk-factors.txt) | diff_sections | BRK-B 10-Q Part II Item 1A across two quarters | The qualified 10-Q key stays within Part II, and the adjacent Item 2 repurchase tables do not leak into the risk-factor diff. |
| [MCP JSON](mcp/10-xom-holding-company.json)<br>[CLI text](cli/10-xom-holding-company.txt) | resolve_company → list_filings → resolve_company | XOM ticker, its 10-Ks, then company-name fallback | The ticker points at a holding company with no 10-K, and the name fallback is empty rather than silently substituting another CIK. |
| [MCP JSON](mcp/11-diff-sections-jpm-legal-proceedings.json)<br>[CLI text](cli/11-diff-sections-jpm-legal-proceedings.txt) | diff_sections | JPM Item 3 across its two latest 10-Ks, max 6 changes | An identical one-paragraph cross-reference is reported honestly as zero changes with similarity 1. |
| [MCP JSON](mcp/12-get-section-aapl-offset.json) | get_section | AAPL 0000320193-25-000079, Item 1A, offset 3, 3 paragraphs | `offset`, `returned`, and `total` make bounded continuation explicit and reproducible. |
